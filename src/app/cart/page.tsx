import { CartPage } from "@/app/cart/cart-page";

export const metadata = {
  title: "Cart | DokKit",
  description: "Review your DokKit cart.",
};

export default function Page() {
  return <CartPage />;
}
